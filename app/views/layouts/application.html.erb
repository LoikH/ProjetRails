<!DOCTYPE html>
<html>
<head>
  <title>ProjetRails</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="principal">

<div id="menu">
	 <%= link_to "Accueil", root_path %> 
	 <%= link_to "Top 10 Utilisateurs", users_path %> 
	 <%= link_to "Top 10 Catégories", root_path %> 
     <%= link_to_if(!session[:user], "S'inscrire", new_user_path) do end %>
	 <%=signin_link%>
</div>

<%= yield %>

<% flash.each do | key, value | %>
<div><%=value %></div>
<% end %>

</div>

<%= debug(params) if Rails.env.development? %>


</body>
</html>
